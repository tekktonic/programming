#include <stdio.h>
#include <stdlib.h>
#include <string.h>

enum opcode {
	MOV,
	ADD,
	DIV,
	SUB,
	MUL
};

enum optype {
	ONEB,
	TWOB,
}
struct op {
	enum optype type;
}
int main(int argc, char **argv)
{
	
}
